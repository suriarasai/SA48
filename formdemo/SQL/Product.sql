CREATE TABLE `sa48`.`product` (
  `id` VARCHAR(100) NOT NULL,
  `name` VARCHAR(50) NULL,
  `type` VARCHAR(50) NULL,
  `category` VARCHAR(50) NULL,
  `description` VARCHAR(100) NULL,
  PRIMARY KEY (`id`));